[project]
name = "forgemind"
version = "0.1.0"
description = "A local-first autonomous AI software engineering agent"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
